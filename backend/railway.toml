[build]
builder = "RAILPACK"

[deploy]
startCommand = "java $JAVA_OPTS -Dserver.port=$PORT -jar $(ls -1 build/libs/*jar | grep -v plain)"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
